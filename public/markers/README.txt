Save your printable marker source image here as `board.png` (or .jpg).

This is the FILE YOU PRINT — phones aim their camera at the printed copy of
this image. The MindAR `.mind` file (in /public/targets/) is the *compiled*
fingerprint that the tracker uses to recognise this image.

Tip: an image with rich, irregular detail tracks better than a clean logo.
